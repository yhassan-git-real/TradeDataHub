<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <!-- Import all theme modules -->
    <ResourceDictionary.MergedDictionaries>
        <!-- Foundation -->
        <ResourceDictionary Source="Foundation/Colors.xaml"/>
        <ResourceDictionary Source="Foundation/Brushes.xaml"/>
        <ResourceDictionary Source="Foundation/Typography.xaml"/>
        <ResourceDictionary Source="Foundation/Tokens.xaml"/>
        
        <!-- Controls -->
        <ResourceDictionary Source="Controls/Buttons.xaml"/>
        <ResourceDictionary Source="Controls/Inputs.xaml"/>
        <ResourceDictionary Source="Controls/Toggles.xaml"/>
        <ResourceDictionary Source="Controls/CustomControls.xaml"/>
        
        <!-- Layout -->
        <ResourceDictionary Source="Layout/Containers.xaml"/>
        
        <!-- Animations -->
        <ResourceDictionary Source="Animations/Animations.xaml"/>
    </ResourceDictionary.MergedDictionaries>

    <!-- Application-specific styles that need to be in the main theme -->
    <!-- Modern Info Card Style with Hover Effects (from MonitoringPanel.xaml) -->
    <Style x:Key="ModernInfoCard" TargetType="Border">
        <Setter Property="Background" Value="{StaticResource Brush.Card.Background}"/>
        <Setter Property="BorderBrush" Value="{StaticResource Brush.Card.Border}"/>
        <Setter Property="BorderThickness" Value="1"/>
        <Setter Property="CornerRadius" Value="8"/>
        <Setter Property="Padding" Value="12,8"/>
        <Setter Property="Cursor" Value="Hand"/>
        <Setter Property="RenderTransformOrigin" Value="0.5,0.5"/>
        <Setter Property="RenderTransform">
            <Setter.Value>
                <ScaleTransform ScaleX="1" ScaleY="1"/>
            </Setter.Value>
        </Setter>
        <Setter Property="Effect">
            <Setter.Value>
                <DropShadowEffect BlurRadius="4" ShadowDepth="1" 
                                  Color="#000000" Opacity="0.08" Direction="270"/>
            </Setter.Value>
        </Setter>
        <Style.Triggers>
            <!-- Fade-in on load -->
            <EventTrigger RoutedEvent="FrameworkElement.Loaded">
                <BeginStoryboard>
                    <Storyboard>
                        <DoubleAnimation Storyboard.TargetProperty="Opacity"
                                       From="0" To="1"
                                       Duration="{StaticResource Animation.Normal}"
                                       EasingFunction="{StaticResource Ease.Decelerate}"/>
                        <DoubleAnimation Storyboard.TargetProperty="(UIElement.RenderTransform).(ScaleTransform.ScaleX)"
                                       From="0.95" To="1"
                                       Duration="{StaticResource Animation.Normal}"
                                       EasingFunction="{StaticResource Ease.Decelerate}"/>
                        <DoubleAnimation Storyboard.TargetProperty="(UIElement.RenderTransform).(ScaleTransform.ScaleY)"
                                       From="0.95" To="1"
                                       Duration="{StaticResource Animation.Normal}"
                                       EasingFunction="{StaticResource Ease.Decelerate}"/>
                    </Storyboard>
                </BeginStoryboard>
            </EventTrigger>
        </Style.Triggers>
    </Style>

    <!-- Modern Status Badge Style -->
    <Style x:Key="ModernStatusBadge" TargetType="Border">
        <Setter Property="Background" Value="{StaticResource Brush.Background.Secondary}"/>
        <Setter Property="BorderBrush" Value="{StaticResource Brush.Border.Light}"/>
        <Setter Property="BorderThickness" Value="1"/>
        <Setter Property="CornerRadius" Value="12"/>
        <Setter Property="Padding" Value="8,4"/>
    </Style>

</ResourceDictionary>
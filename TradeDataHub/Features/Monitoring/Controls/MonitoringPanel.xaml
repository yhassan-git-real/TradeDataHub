<UserControl x:Class="TradeDataHub.Features.Monitoring.Controls.MonitoringPanel"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             d:DesignHeight="450" d:DesignWidth="800">
    
    <UserControl.Resources>
        <!-- Modern Status Badge Style -->
        <Style x:Key="ModernStatusBadge" TargetType="Border">
            <Setter Property="CornerRadius" Value="12"/>
            <Setter Property="Padding" Value="8,4"/>
            <Setter Property="MinWidth" Value="80"/>
            <Setter Property="HorizontalAlignment" Value="Left"/>
        </Style>
        
        <!-- Log Entry Style with Alternating Rows -->
        <Style x:Key="ModernLogEntry" TargetType="ListViewItem">
            <Setter Property="Padding" Value="8,6"/>
            <Setter Property="Margin" Value="0"/>
            <Setter Property="BorderThickness" Value="0,0,0,1"/>
            <Setter Property="BorderBrush" Value="{StaticResource Brush.Border.Light}"/>
            <Setter Property="FontFamily" Value="{StaticResource PrimaryFontFamily}"/>
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
            <Style.Triggers>
                <!-- Alternating Row Colors -->
                <Trigger Property="ItemsControl.AlternationIndex" Value="0">
                    <Setter Property="Background" Value="{StaticResource Brush.Background}"/>
                </Trigger>
                <Trigger Property="ItemsControl.AlternationIndex" Value="1">
                    <Setter Property="Background" Value="{StaticResource Brush.Background.Secondary}"/>
                </Trigger>
                
                <!-- Hover Effect -->
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="{StaticResource Brush.Background.Tertiary}"/>
                    <Setter Property="BorderBrush" Value="{StaticResource Brush.Border}"/>
                </Trigger>
                
                <!-- Selected State -->
                <Trigger Property="IsSelected" Value="True">
                    <Setter Property="Background" Value="{StaticResource Brush.Primary}"/>
                    <Setter Property="Foreground" Value="{StaticResource Brush.Text.OnPrimary}"/>
                    <Setter Property="BorderBrush" Value="{StaticResource Brush.Primary}"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <!-- Column Header Style -->
        <Style x:Key="ModernColumnHeader" TargetType="GridViewColumnHeader">
            <Setter Property="FontFamily" Value="{StaticResource PrimaryFontFamily}"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="FontSize" Value="11"/>
            <Setter Property="Foreground" Value="{StaticResource Brush.Text.Secondary}"/>
            <Setter Property="Background" Value="{StaticResource Brush.Background.Secondary}"/>
            <Setter Property="BorderBrush" Value="{StaticResource Brush.Border.Light}"/>
            <Setter Property="BorderThickness" Value="0,0,1,0"/>
            <Setter Property="Padding" Value="8,6"/>
            <Setter Property="HorizontalContentAlignment" Value="Left"/>
        </Style>
        
        <!-- Modern Info Card Style with Hover Effects -->
        <Style x:Key="ModernInfoCard" TargetType="Border">
            <Setter Property="Background" Value="{StaticResource Brush.Background.Tertiary}"/>
            <Setter Property="BorderBrush" Value="{StaticResource Brush.Border.Light}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="CornerRadius" Value="6"/>
            <Setter Property="Padding" Value="12,8"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="RenderTransformOrigin" Value="0.5,0.5"/>
            <Setter Property="RenderTransform">
                <Setter.Value>
                    <ScaleTransform ScaleX="1" ScaleY="1"/>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <!-- Fade-in on load -->
                <EventTrigger RoutedEvent="FrameworkElement.Loaded">
                    <BeginStoryboard>
                        <Storyboard>
                            <DoubleAnimation Storyboard.TargetProperty="Opacity"
                                           From="0" To="1"
                                           Duration="{StaticResource Animation.Normal}"
                                           EasingFunction="{StaticResource Ease.Decelerate}"/>
                            <DoubleAnimation Storyboard.TargetProperty="(UIElement.RenderTransform).(ScaleTransform.ScaleX)"
                                           From="0.95" To="1"
                                           Duration="{StaticResource Animation.Normal}"
                                           EasingFunction="{StaticResource Ease.Decelerate}"/>
                            <DoubleAnimation Storyboard.TargetProperty="(UIElement.RenderTransform).(ScaleTransform.ScaleY)"
                                           From="0.95" To="1"
                                           Duration="{StaticResource Animation.Normal}"
                                           EasingFunction="{StaticResource Ease.Decelerate}"/>
                        </Storyboard>
                    </BeginStoryboard>
                </EventTrigger>
                
                <!-- Enhanced Hover Effects with Animation -->
                <Trigger Property="IsMouseOver" Value="True">
                    <Trigger.EnterActions>
                        <BeginStoryboard>
                            <Storyboard>
                                <ColorAnimation Storyboard.TargetProperty="(Border.Background).(SolidColorBrush.Color)"
                                              To="{StaticResource Color.Neutral.Gray20}"
                                              Duration="{StaticResource Animation.Fast}"
                                              EasingFunction="{StaticResource Ease.Standard}"/>
                                <ColorAnimation Storyboard.TargetProperty="(Border.BorderBrush).(SolidColorBrush.Color)"
                                              To="{StaticResource Color.Primary}"
                                              Duration="{StaticResource Animation.Fast}"
                                              EasingFunction="{StaticResource Ease.Standard}"/>
                                <DoubleAnimation Storyboard.TargetProperty="(UIElement.RenderTransform).(ScaleTransform.ScaleX)"
                                               To="1.02"
                                               Duration="{StaticResource Animation.Fast}"
                                               EasingFunction="{StaticResource Ease.Standard}"/>
                                <DoubleAnimation Storyboard.TargetProperty="(UIElement.RenderTransform).(ScaleTransform.ScaleY)"
                                               To="1.02"
                                               Duration="{StaticResource Animation.Fast}"
                                               EasingFunction="{StaticResource Ease.Standard}"/>
                            </Storyboard>
                        </BeginStoryboard>
                    </Trigger.EnterActions>
                    <Trigger.ExitActions>
                        <BeginStoryboard>
                            <Storyboard>
                                <ColorAnimation Storyboard.TargetProperty="(Border.Background).(SolidColorBrush.Color)"
                                              Duration="{StaticResource Animation.Fast}"
                                              EasingFunction="{StaticResource Ease.Standard}"/>
                                <ColorAnimation Storyboard.TargetProperty="(Border.BorderBrush).(SolidColorBrush.Color)"
                                              Duration="{StaticResource Animation.Fast}"
                                              EasingFunction="{StaticResource Ease.Standard}"/>
                                <DoubleAnimation Storyboard.TargetProperty="(UIElement.RenderTransform).(ScaleTransform.ScaleX)"
                                               To="1"
                                               Duration="{StaticResource Animation.Fast}"
                                               EasingFunction="{StaticResource Ease.Standard}"/>
                                <DoubleAnimation Storyboard.TargetProperty="(UIElement.RenderTransform).(ScaleTransform.ScaleY)"
                                               To="1"
                                               Duration="{StaticResource Animation.Fast}"
                                               EasingFunction="{StaticResource Ease.Standard}"/>
                            </Storyboard>
                        </BeginStoryboard>
                    </Trigger.ExitActions>
                </Trigger>
            </Style.Triggers>
        </Style>
    </UserControl.Resources>

    <Grid Margin="12">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Modern System Monitor Section -->
        <Border Grid.Row="0" Background="{StaticResource Brush.Background.Secondary}" 
                BorderBrush="{StaticResource Brush.Border.Light}" BorderThickness="1" 
                Padding="20,16,20,16" CornerRadius="6,6,0,0"
                Margin="0,0,0,0" MinHeight="80">
            <Border.Effect>
                <DropShadowEffect Color="Black" ShadowDepth="1" BlurRadius="3" Opacity="0.06"/>
            </Border.Effect>
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <!-- System Status Section -->
                <StackPanel Grid.Column="0" VerticalAlignment="Center" Margin="0,0,24,0" MinWidth="200">
                    <StackPanel Orientation="Horizontal" Margin="0,0,0,6">
                        <Border x:Name="StatusBadge" Style="{StaticResource ModernStatusBadge}">
                            <StackPanel Orientation="Horizontal">
                                <Ellipse x:Name="StatusIndicator" Width="8" Height="8" 
                                         Margin="0,0,6,0" VerticalAlignment="Center"/>
                                <TextBlock x:Name="StatusText" Text="Idle" 
                                           Style="{StaticResource Typography.Caption}"
                                           FontWeight="SemiBold" Foreground="White"/>
                            </StackPanel>
                        </Border>
                    </StackPanel>
                    <TextBlock x:Name="StatusMessage" Text="Application ready" 
                               Style="{StaticResource Typography.Body}" 
                               Foreground="{StaticResource Brush.Text.Secondary}"
                               Margin="0,0,0,2"/>
                </StackPanel>

                <!-- Combined Information and Progress Section -->
                <StackPanel Grid.Column="1" VerticalAlignment="Center" Margin="0,0,24,0">
                    <!-- Modern Info Cards Section -->
                    <StackPanel Orientation="Horizontal" Margin="0,0,0,8" HorizontalAlignment="Left">
                        <!-- File Status Card -->
                        <Border Style="{StaticResource ModernInfoCard}" Margin="0,0,8,0" MinWidth="110">
                            <StackPanel>
                                <TextBlock Text="FILE STATUS" Style="{StaticResource Typography.Caption}" 
                                           Foreground="{StaticResource Brush.Primary}" 
                                           FontWeight="Bold" FontSize="9" Margin="0,0,0,3"/>
                                <TextBlock x:Name="CurrentFileText" Text="No file active" 
                                           Style="{StaticResource Typography.Label}" 
                                           FontWeight="SemiBold" Foreground="{StaticResource Brush.Text.Primary}"/>
                            </StackPanel>
                        </Border>
                        
                        <!-- Records Card -->
                        <Border Style="{StaticResource ModernInfoCard}" Margin="0,0,8,0" MinWidth="90">
                            <StackPanel>
                                <TextBlock Text="RECORDS" Style="{StaticResource Typography.Caption}" 
                                           Foreground="{StaticResource Brush.Primary}" 
                                           FontWeight="Bold" FontSize="9" Margin="0,0,0,3"/>
                                <TextBlock x:Name="RecordCountText" Text="0 records" 
                                           Style="{StaticResource Typography.Label}" 
                                           FontWeight="SemiBold" Foreground="{StaticResource Brush.Text.Primary}"/>
                            </StackPanel>
                        </Border>
                        
                        <!-- Time Card -->
                        <Border Style="{StaticResource ModernInfoCard}" Margin="0,0,0,0" MinWidth="90">
                            <StackPanel>
                                <TextBlock Text="ELAPSED" Style="{StaticResource Typography.Caption}" 
                                           Foreground="{StaticResource Brush.Primary}" 
                                           FontWeight="Bold" FontSize="9" Margin="0,0,0,3"/>
                                <TextBlock x:Name="ElapsedTimeText" Text="00:00:000" 
                                           Style="{StaticResource Typography.Label}" 
                                           FontWeight="SemiBold" Foreground="{StaticResource Brush.Text.Primary}"/>
                            </StackPanel>
                        </Border>
                    </StackPanel>
                    
                    <!-- Progress Bar -->
                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Left">
                        <ProgressBar x:Name="ProgressBar" Width="200" Height="6" 
                                     Minimum="0" Maximum="100" Value="0" 
                                     Background="{StaticResource Brush.Border.Light}" 
                                     Foreground="{StaticResource Brush.Primary}"
                                     BorderThickness="0"/>
                        <TextBlock x:Name="ProgressText" Text="0%" 
                                   Style="{StaticResource Typography.Caption}" 
                                   Foreground="{StaticResource Brush.Text.Secondary}"
                                   Margin="8,0,0,0" 
                                   VerticalAlignment="Center" FontWeight="SemiBold"/>
                    </StackPanel>
                </StackPanel>

                <!-- Control Buttons -->
                <StackPanel Grid.Column="2" Orientation="Horizontal" VerticalAlignment="Center"
                            HorizontalAlignment="Right" Margin="0,0,0,0">
                    <Button x:Name="ClearLogsButton" Content="Clear" Width="76" Height="36" 
                            Click="ClearLogsButton_Click" Style="{StaticResource NeutralButton}"
                            Margin="0,0,8,0" FontSize="11" FontWeight="SemiBold"/>
                    <ToggleButton x:Name="AutoScrollToggle" Content="Auto" Width="76" Height="36" 
                                  IsChecked="True" Click="AutoScrollToggle_Click"
                                  Style="{StaticResource ModernToggleButton}" FontSize="11" FontWeight="SemiBold"/>
                </StackPanel>
            </Grid>
        </Border>

        <!-- Modern Activity Monitor Header -->
        <Border x:Name="ActivityMonitorHeader"
                Grid.Row="1" Background="{StaticResource Brush.Background}" 
                BorderBrush="{StaticResource Brush.Border.Light}" 
                Padding="16,12">
            <Border.Style>
                <Style TargetType="Border">
                    <Setter Property="BorderThickness" Value="1,0,1,0"/>
                    <Style.Triggers>
                        <DataTrigger Binding="{Binding IsExpanded}" Value="False">
                            <Setter Property="BorderThickness" Value="1,0,1,1"/>
                            <Setter Property="CornerRadius" Value="0,0,6,6"/>
                        </DataTrigger>
                    </Style.Triggers>
                </Style>
            </Border.Style>
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                
                <StackPanel Grid.Column="0" Orientation="Horizontal" VerticalAlignment="Center">
                    <TextBlock Text="Activity Monitor" 
                               Style="{StaticResource Typography.Header3}" 
                               Margin="0,0,12,0"/>
                    <TextBlock x:Name="LogCountText" Text="0 entries" 
                               Style="{StaticResource Typography.Caption}" 
                               Foreground="{StaticResource Brush.Text.Secondary}"
                               VerticalAlignment="Center"/>
                </StackPanel>
                
                <TextBlock Grid.Column="1" Text="Real-time processing logs and system activity" 
                           Style="{StaticResource Typography.Caption}"
                           Foreground="{StaticResource Brush.Text.Tertiary}"
                           Margin="8,0,0,0" 
                           VerticalAlignment="Center"
                           HorizontalAlignment="Right"/>

                <!-- Expand/Collapse Toggle Button -->
                <Button x:Name="ExpandCollapseButton" 
                        Grid.Column="2" 
                        Width="32" Height="32"
                        Margin="8,0,0,0"
                        Background="Transparent"
                        BorderThickness="0"
                        Click="ExpandCollapseButton_Click"
                        Cursor="Hand"
                        ToolTip="Expand/Collapse Activity Monitor">
                    <Button.Content>
                        <Border Background="{StaticResource Brush.Background.Secondary}"
                                CornerRadius="16"
                                Width="28" Height="28">
                            <TextBlock x:Name="ExpandCollapseIcon"
                                       Text="▼"
                                       FontSize="10"
                                       Foreground="{StaticResource Brush.Text.Primary}"
                                       HorizontalAlignment="Center"
                                       VerticalAlignment="Center"
                                       RenderTransformOrigin="0.5,0.5">
                                <TextBlock.RenderTransform>
                                    <RotateTransform x:Name="IconRotateTransform" Angle="0"/>
                                </TextBlock.RenderTransform>
                            </TextBlock>
                        </Border>
                    </Button.Content>
                    <Button.Style>
                        <Style TargetType="Button">
                            <Setter Property="Template">
                                <Setter.Value>
                                    <ControlTemplate TargetType="Button">
                                        <ContentPresenter/>
                                        <ControlTemplate.Triggers>
                                            <Trigger Property="IsMouseOver" Value="True">
                                                <Trigger.EnterActions>
                                                    <BeginStoryboard>
                                                        <Storyboard>
                                                            <DoubleAnimation Storyboard.TargetProperty="(UIElement.RenderTransform).(ScaleTransform.ScaleX)"
                                                                           To="1.1"
                                                                           Duration="{StaticResource Animation.Fast}"
                                                                           EasingFunction="{StaticResource Ease.Standard}"/>
                                                            <DoubleAnimation Storyboard.TargetProperty="(UIElement.RenderTransform).(ScaleTransform.ScaleY)"
                                                                           To="1.1"
                                                                           Duration="{StaticResource Animation.Fast}"
                                                                           EasingFunction="{StaticResource Ease.Standard}"/>
                                                        </Storyboard>
                                                    </BeginStoryboard>
                                                </Trigger.EnterActions>
                                                <Trigger.ExitActions>
                                                    <BeginStoryboard>
                                                        <Storyboard>
                                                            <DoubleAnimation Storyboard.TargetProperty="(UIElement.RenderTransform).(ScaleTransform.ScaleX)"
                                                                           To="1.0"
                                                                           Duration="{StaticResource Animation.Fast}"
                                                                           EasingFunction="{StaticResource Ease.Standard}"/>
                                                            <DoubleAnimation Storyboard.TargetProperty="(UIElement.RenderTransform).(ScaleTransform.ScaleY)"
                                                                           To="1.0"
                                                                           Duration="{StaticResource Animation.Fast}"
                                                                           EasingFunction="{StaticResource Ease.Standard}"/>
                                                        </Storyboard>
                                                    </BeginStoryboard>
                                                </Trigger.ExitActions>
                                            </Trigger>
                                        </ControlTemplate.Triggers>
                                    </ControlTemplate>
                                </Setter.Value>
                            </Setter>
                            <Setter Property="RenderTransformOrigin" Value="0.5,0.5"/>
                            <Setter Property="RenderTransform">
                                <Setter.Value>
                                    <ScaleTransform ScaleX="1" ScaleY="1"/>
                                </Setter.Value>
                            </Setter>
                        </Style>
                    </Button.Style>
                </Button>
            </Grid>
        </Border>

        <!-- Modern Logs Viewer Section -->
        <Border x:Name="LogsViewerBorder"
                Grid.Row="2" Background="{StaticResource Brush.Background}" 
                BorderBrush="{StaticResource Brush.Border.Light}" 
                BorderThickness="1,0,1,1" CornerRadius="0,0,6,6"
                ClipToBounds="True">
            <Border.Style>
                <Style TargetType="Border" BasedOn="{StaticResource FadeInAnimation}">
                    <Setter Property="Height" Value="200"/>
                    <Style.Triggers>
                        <DataTrigger Binding="{Binding IsExpanded}" Value="False">
                            <DataTrigger.EnterActions>
                                <BeginStoryboard x:Name="CollapseStoryboard">
                                    <Storyboard>
                                        <DoubleAnimation Storyboard.TargetProperty="Height"
                                                       To="0"
                                                       Duration="{StaticResource Animation.Normal}"
                                                       EasingFunction="{StaticResource Ease.Standard}"/>
                                        <DoubleAnimation Storyboard.TargetProperty="Opacity"
                                                       To="0"
                                                       Duration="{StaticResource Animation.Fast}"
                                                       EasingFunction="{StaticResource Ease.Standard}"/>
                                    </Storyboard>
                                </BeginStoryboard>
                            </DataTrigger.EnterActions>
                            <DataTrigger.ExitActions>
                                <BeginStoryboard>
                                    <Storyboard>
                                        <DoubleAnimation Storyboard.TargetProperty="Height"
                                                       To="200"
                                                       Duration="{StaticResource Animation.Normal}"
                                                       EasingFunction="{StaticResource Ease.Standard}"/>
                                        <DoubleAnimation Storyboard.TargetProperty="Opacity"
                                                       To="1"
                                                       Duration="{StaticResource Animation.Normal}"
                                                       EasingFunction="{StaticResource Ease.Standard}"/>
                                    </Storyboard>
                                </BeginStoryboard>
                            </DataTrigger.ExitActions>
                        </DataTrigger>
                    </Style.Triggers>
                </Style>
            </Border.Style>
            <ListView x:Name="LogsListView" 
                      Background="Transparent" BorderThickness="0"
                      ScrollViewer.HorizontalScrollBarVisibility="Disabled"
                      ScrollViewer.VerticalScrollBarVisibility="Visible"
                      ScrollViewer.CanContentScroll="True"
                      VirtualizingPanel.IsVirtualizing="True"
                      VirtualizingPanel.VirtualizationMode="Recycling"
                      AlternationCount="2"
                      ItemContainerStyle="{StaticResource ModernLogEntry}">
                    <ListView.View>
                        <GridView>
                            <GridViewColumn Header="Time" Width="95">
                                <GridViewColumn.HeaderContainerStyle>
                                    <StaticResource ResourceKey="ModernColumnHeader"/>
                                </GridViewColumn.HeaderContainerStyle>
                                <GridViewColumn.CellTemplate>
                                    <DataTemplate>
                                        <Border Background="{StaticResource Brush.Background.Tertiary}" 
                                                CornerRadius="4" Padding="6,3">
                                            <TextBlock Text="{Binding FormattedTimestamp}" 
                                                       FontSize="10" 
                                                       FontFamily="Consolas, monospace" 
                                                       Foreground="{StaticResource Brush.Text.Primary}"
                                                       FontWeight="Medium" HorizontalAlignment="Center"/>
                                        </Border>
                                    </DataTemplate>
                                </GridViewColumn.CellTemplate>
                            </GridViewColumn>
                            
                            <GridViewColumn Header="Level" Width="70">
                                <GridViewColumn.HeaderContainerStyle>
                                    <StaticResource ResourceKey="ModernColumnHeader"/>
                                </GridViewColumn.HeaderContainerStyle>
                                <GridViewColumn.CellTemplate>
                                    <DataTemplate>
                                        <Border Background="{Binding LevelColor}" 
                                                CornerRadius="3" Padding="6,2" MinWidth="45">
                                            <TextBlock Text="{Binding LevelText}" 
                                                       FontSize="9" FontWeight="Bold" 
                                                       Foreground="White" 
                                                       HorizontalAlignment="Center"
                                                       FontFamily="{StaticResource PrimaryFontFamily}"/>
                                        </Border>
                                    </DataTemplate>
                                </GridViewColumn.CellTemplate>
                            </GridViewColumn>
                            
                            <GridViewColumn Header="Source" Width="120">
                                <GridViewColumn.HeaderContainerStyle>
                                    <StaticResource ResourceKey="ModernColumnHeader"/>
                                </GridViewColumn.HeaderContainerStyle>
                                <GridViewColumn.CellTemplate>
                                    <DataTemplate>
                                        <TextBlock Text="{Binding Source}" 
                                                   Style="{StaticResource Typography.Caption}"
                                                   Foreground="{StaticResource Brush.Text.Secondary}"
                                                   FontWeight="SemiBold"/>
                                    </DataTemplate>
                                </GridViewColumn.CellTemplate>
                            </GridViewColumn>
                            
                            <GridViewColumn Header="Message" Width="440">
                                <GridViewColumn.HeaderContainerStyle>
                                    <StaticResource ResourceKey="ModernColumnHeader"/>
                                </GridViewColumn.HeaderContainerStyle>
                                <GridViewColumn.CellTemplate>
                                    <DataTemplate>
                                        <Border Padding="4,2">
                                            <TextBlock Text="{Binding Message}" 
                                                       Style="{StaticResource Typography.Body}"
                                                       TextWrapping="Wrap" 
                                                       ToolTip="{Binding Message}"
                                                       MaxWidth="420"
                                                       LineHeight="16"
                                                       Foreground="{StaticResource Brush.Text.Primary}"/>
                                        </Border>
                                    </DataTemplate>
                                </GridViewColumn.CellTemplate>
                            </GridViewColumn>
                        </GridView>
                    </ListView.View>
                </ListView>
        </Border>
    </Grid>
</UserControl>
